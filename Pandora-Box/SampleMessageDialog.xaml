<UserControl x:Class="Pandora_Box.SampleMessageDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:local="clr-namespace:Pandora_Box"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <!--来自MaterialDesign的对话框风格-->
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock 
            x:Name="Message"  HorizontalAlignment="Center" VerticalAlignment="Center"
            Margin="0 6 0 0" FontSize="14" Grid.Row="0"
            />
        <Button 
            Grid.Row="1" HorizontalAlignment="Right" Margin="16 16 16 0" 
            IsDefault="True" Style="{DynamicResource MaterialDesignFlatButton}" 
            Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
            我知道了！
        </Button>
    </Grid>
</UserControl>
